CREATE DATABASE libreria;

----------------------------------------------------------

-- Tabla Autores
CREATE TABLE autores (
    id SERIAL PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    apellido VARCHAR(100) NOT NULL,
    nacionalidad VARCHAR(50),
    biografia TEXT,
    fecha_nacimiento DATE
);

-- Tabla Libros
CREATE TABLE libros (
    id SERIAL PRIMARY KEY,
    titulo VARCHAR(200) NOT NULL,
    autor_id INT REFERENCES autores(id) ON DELETE CASCADE,
    fecha_publicacion DATE,
    genero VARCHAR(50),
    isbn VARCHAR(20) UNIQUE,
    precio DECIMAL(10, 2),
    stock INT DEFAULT 0
);

CREATE TABLE usuarios (
    id SERIAL PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password TEXT NOT NULL,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


-- Datos de ejemplo
INSERT INTO autores (nombre, apellido, nacionalidad) VALUES 
('Gabriel', 'García Márquez', 'Colombiana'),
('Isabel', 'Allende', 'Chilena'),
('Mario', 'Vargas Llosa', 'Peruana');

INSERT INTO libros (titulo, autor_id, genero, precio, stock) VALUES 
('Cien años de soledad', 1, 'Novela', 25.50, 10),
('La casa de los espíritus', 2, 'Ficción', 18.90, 5),
('La ciudad y los perros', 3, 'Drama', 15.00, 8);
